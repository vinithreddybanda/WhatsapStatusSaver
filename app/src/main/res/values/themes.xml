<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Your Main App Theme -->
    <style name="Theme.WhatsapStatus" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Your Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="android:windowFullscreen">true</item>
        <!-- Screen Background -->
        <item name="windowSplashScreenBackground">#FF0B141A</item>

        <!-- Point to the NEW resized wrapper -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/tenten</item>

        <item name="postSplashScreenTheme">@style/Theme.WhatsapStatus</item>
    </style>
</resources>
